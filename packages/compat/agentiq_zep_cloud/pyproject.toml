[build-system]
build-backend = "setuptools.build_meta"
requires = ["setuptools >= 64", "setuptools-scm>=8"]

[tool.setuptools_scm]
root = "../../.."

[project]
name = "agentiq-zep-cloud"
dynamic = ["version"]
dependencies = [
  "aiqtoolkit-zep-cloud"
]
readme = "pypi.md"
description = "Transitional package for aiqtoolkit-zep-cloud, this package is deprecated and will be removed in the future."
classifiers = ["Programming Language :: Python"]
